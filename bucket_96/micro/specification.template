DEF[PORTVERSION]=	2.0.11
# ----------------------------------------------------------------------------

NAMEBASE=		micro
VERSION=		${PORTVERSION}
KEYWORDS=		editors
VARIANTS=		standard
SDESC[standard]=	Terminal based text editor in go
HOMEPAGE=		none
CONTACT=		Jay_Leno[jay@aarp.org]

DOWNLOAD_GROUPS=	main
SITES[main]=		GITHUB/zyedidia:micro:v${PORTVERSION}
DISTFILE[1]=		generated:main

SPKGS[standard]=	single

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

FPC_EQUIVALENT=		editors/micro
